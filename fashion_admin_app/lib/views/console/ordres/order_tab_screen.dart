import 'package:flutter/material.dart';

class OrderTabScreen extends StatelessWidget {
  const OrderTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('hi i am order'),),);
  }
}